name: Branchyng Strategy PRE
on:
  pull_request:
    types: [ assigned, opened, synchronize ]
    branches:
      - preproduccion/*
jobs:
  Current-Branch:
    runs-on: ubuntu-latest
    steps:
      - name: Extract branch name
        shell: bash
        run: echo "##[set-output name=branch;]$(echo ${GITHUB_HEAD_REF#refs/heads/})"
        id: extract_branch
      - uses: jungwinter/split@v2
        id: split
        with:
          msg: '${{ steps.extract_branch.outputs.branch }}'
          separator: '/'
    outputs: 
      branch: ${{ steps.extract_branch.outputs.branch }}
      proyect: ${{ steps.split.outputs._1 }}
  Branching-Fork:
    name: Branching-Fork
    needs: Current-Branch
    uses: branching-strategy-brian/DevOps-Workflow/.github/workflows/preproduccion.yml@
    with: 
      environment: preproduccion/${{needs.Current-Branch.outputs.proyect}}
    secrets:
      TOKEN: ${{ secrets.TOKEN }}